#include "headers/parser.h"

void parseData(string data) {
  cout << "[I] Entering parseData()\n";
  cout << data << endl;
}

void parseCommand(string cmd) {
  cout << "[I] Entering parseCommand()\n";
  cout << cmd << endl;
}
